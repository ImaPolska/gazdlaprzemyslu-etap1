# Decyzje i założenia (etap 1)

Format: data · decyzja · uzasadnienie · konsekwencja. Decyzje domyślne z sekcji 1.3 instrukcji obowiązują bez wpisu; poniżej tylko to, co wymagało rozstrzygnięcia.

## Dzień 1 (2026-09-13) – decyzje startowe (P1.0)

| # | Decyzja | Uzasadnienie | Konsekwencja |
|---|---|---|---|
| D1 | **Środowisko: WordPress Playground CLI** (`@wp-playground/cli`, Node 20) jako runtime lokalny; brak PHP, Dockera i wp-env w sandboxie. Weryfikacja headless: Playwright (Chromium) + `urllib`. | Jedyne dostępne środowisko uruchamiające WordPress; identyczny silnik jak playground.wordpress.net, więc test lokalny = test linku. | Blueprinty testowe (`blueprints/dev/*.json`, nieversjonowane) czytają artefakty z lokalnego serwera plików; produkcyjne – z `raw.githubusercontent.com` na tagu. |
| D2 | **theme.json jest jedynym źródłem tokenów** (kolory, fonty, rozmiary, odstępy, `contentSize` 760 / `wideSize` 1200, `custom:false`). Customizer Blocksy dostaje te same wartości (hex, `var(--wp--preset--font-family--…)`) przez `config/theme_mods_X.json`, generowany skryptem z tych samych plików tokenów (`theme/gdp-child/variants/X/tokens.json`). | Blocksy jest motywem hybrydowym: nagłówek, stopka, typografia globalna i tło idą z Customizera, edytor i bloki z theme.json. Jedna definicja → brak rozjazdu. | Zmiana tokenu = edycja `tokens.json` + `scripts/build-all.sh`. Nic nie jest ustawiane ręcznie w Customizerze. |
| D3 | **Fonty self-hosted (WOFF2, subset latin + latin-ext), ładowane przez `fontFace` w theme.json; Google Fonts w Blocksy wyłączone filtrem.** A: IBM Plex Sans + IBM Plex Mono; B: Manrope + Source Sans 3 (mono: IBM Plex Mono); C: Source Serif 4 + Inter (mono: IBM Plex Mono). | Zero zewnętrznych hostów (13.5), RODO, licencje OFL. | ZIP kierunku zawiera tylko fonty użyte w jego theme.json (170–400 KB). |
| D4 | **Bez Blocksy Companion i bez jakichkolwiek wtyczek.** | Wymóg instrukcji; wszystko, czego brakuje w Blocksy free, dostarcza motyw potomny (CSS/JS/PHP). | Sticky + shrink nagłówka (w Blocksy free tylko w Pro) zrealizowane w `assets/js/header.js` + CSS. Brak formularzy – sloty są atrapami z bloków core (`[[slot]]`). |
| D5 | **Jeden katalog motywu potomnego, trzy warianty tokenów → trzy ZIP-y** (`dist/gdp-child-A|B|C.zip`); różnią się `theme.json`, `direction.txt` i zestawem fontów. CSS/PHP/wzorce wspólne, różnice stylistyczne przez klasę `body.gdp-dir-X` i zmienne `--gdp-*`. | Po wyborze kierunku wystarczy usunąć dwa warianty; brak trzech rozjeżdżających się motywów. | `main.json` (P1.2) powstanie z wybranego wariantu bez zmian struktury. |
| D6 | **Treść jest identyczna w A/B/C; różni się wyłącznie sekcja hero strony głównej** (`content/pages/start.hero.X.html`: A – dwie kolumny z formularzem; B – ciemna „okładka” jako `core/group` z kolorem tła, nie `core/cover`; C – redakcyjny układ z boksem „Najnowszy komentarz rynkowy”). Trzy WXR generowane z jednego źródła. | Wymóg porównania kierunków na tej samej treści. | Hero A/B/C to trzy wzorce; po wyborze pozostaje jeden. |
| D7 | **Menu i widgety stopki odtwarza `scripts/apply-config.php` z `config/menus.json` i `config/footer.json` po imporcie WXR**, a nie WXR. Odwołania `wp_block` w treści zapisane jako `__BLOCK:slug__` i mapowane na ID po imporcie. | Importer WXR nie gwarantuje zachowania ID `wp_block` ani przypisań lokalizacji menu. | Jedno źródło menu/stopki dla Playground i dla `restore-on-host.sh` (etap 2). |
| D8 | **Hero Blocksy (tytuł strony) wyłączony dla stron; h1 jest w treści strony** (pierwszy blok sekcji „Hero”/„Nagłówek strony”). Wyjątek: archiwum `/wiedza/` (brak treści strony) – h1 z hero Blocksy, tylko tytuł. | Dokładnie jeden h1 na stronę (13.1), pełna kontrola redakcyjna nad nagłówkiem. | Redaktor zmienia h1 w edytorze, nie w Customizerze. |
| D9 | **Stron zastępczych („stub”) jest 23 – każdy URL z mapy serwisu istnieje** (nagłówek, jedno zdanie, `[[treść]]`), kategorie 6, wpisy 2 (szablony komentarza rynkowego). | 13.2 wymaga HTTP 200 dla całej mapy już w P1.1. | Pełna treść stron poza `start` i `cena-stala` – P1.2. |
| D10 | **`/komentarz-rynkowy/` jest stroną z Query Loop po kategorii „Komentarz rynkowy”**, nie archiwum kategorii; archiwa kategorii są pod `/category/…/`. | Zmiana bazy kategorii na pusty prefiks wymaga wtyczki lub ingerencji w rewrite. | Adresy archiwów: `/category/<slug>/`; strona zbiorcza `/komentarz-rynkowy/`. |
| D11 | **Blokady edycji**: każda sekcja nadrzędna `templateLock: contentOnly`; `canLockBlocks:false` i `codeEditingEnabled:false` dla roli Editor; lista dozwolonych bloków = `core/*` bez `core/html`, `core/freeform`, `core/shortcode`, `core/code`. | Sekcja 5 i 14 instrukcji. | Redaktor zmienia teksty, linki, obrazy; nie zmienia struktury. |
| D12 | **Język instalacji pl_PL przez `setSiteLanguage`**; brakujące polskie etykiety Blocksy (aria-label) uzupełnia filtr `gettext_blocksy` w `functions.php`. | Pakiet pl_PL dla Blocksy w translate.wordpress.org jest niekompletny („File not found” przy pobieraniu). | Panel i front po polsku bez wtyczek. |
| D13 | **`blog_public = 0` w podglądach** (Playground i staging); produkcja `1` po akceptacji P1.4. | Prototypy nie mogą być indeksowane. | Wpis w `restore-on-host.sh`. |
| D14 | **Użytkownik `redaktor` (rola Editor) tworzony w blueprincie przez WP-CLI, hasło `Redaktor-Etap1-2026`** (stałe, jawne – instancje Playground są jednorazowe i lokalne w przeglądarce). | Sekcja 11.2. | Hasło zmienia się w P1.2 razem z `main.json`, jeżeli zleceniodawca zażyczy. |
| D15 | **Tabele: `hasFixedLayout:false`, przewijanie poziome w kontenerze tabeli poniżej 782 px, font mono tylko w kolumnach liczbowych** (`.gdp-liczby td:nth-child(2|3)`). | 13.4 dopuszcza przewijanie wyłącznie w kontenerze tabeli; mono w tekście obniża czytelność. | Tabele porównawcze zawsze z klasą `gdp-liczby`, jeżeli kolumny 2–3 są liczbowe. |
| D16 | **Zrzuty referencyjne: 1440×900 (desktop) i 360×800 (mobile), pełna strona, widok niezalogowanego gościa** (`scripts/screenshot.py`). | Zgodnie z 13.4; pasek admina nie zniekształca layoutu. | Zrzuty 768×1024 dołączane od P1.2. |
| D17 | **Plan dni do P1.1 (dzień 1–4)**: d1 – środowisko, skille, motyw potomny, tokeny, blueprinty, skrypty weryfikacji; d2 – wzorce zsynchronizowane i niezsynchronizowane, treść `start` + `cena-stala`, stopka/menu; d3 – trzy kierunki, QA wizualne, poprawki; d4 – tagi `etap1-A/B/C`, test linków publicznych, raport P1.1. Wykonanie: całość zrealizowana w dniu 1; publikacja tagów czeka na repozytorium GitHub. | Praca bez pytań między punktami kontrolnymi. | Po wyborze kierunku: P1.2 w 5 dni roboczych od decyzji. |
| D18 | **Repozytorium**: agent nie ma własnego konta GitHub (brak tokenu w sandboxie), więc publiczne repozytorium musi powstać na koncie zleceniodawcy (połączenie GitHub w tej sesji albo podany URL repo z prawem zapisu). Do czasu publikacji artefakty są kompletne lokalnie (tagi lokalne `etap1-A/B/C`). | Linki `playground.wordpress.net/?blueprint-url=…` wymagają publicznego URL z CORS – `raw.githubusercontent.com` spełnia to bez konfiguracji. | Po podłączeniu GitHub: `git push --tags`, przebudowa blueprintów produkcyjnych z właściwym `--base`, test linków. |
